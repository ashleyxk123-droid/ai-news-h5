{
  "name": "ai-news-h5",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "concurrently -n server,frontend -c blue,green \"npm run dev -w packages/server\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/frontend",
    "build:static": "npm run generate-data && npm run build:static -w packages/frontend",
    "generate-data": "npx tsx scripts/generate-data.ts",
    "start": "npm run start -w packages/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.5.0"
  }
}
